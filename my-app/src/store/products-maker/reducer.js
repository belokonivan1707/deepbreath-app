import PRODUCTS_MAKERS from "./data";

const INITIAL_STATE = {
  productsMakers: PRODUCTS_MAKERS,
};

const productMakersReducer = (state = INITIAL_STATE) => {
  switch (true) {
    default:
      return state;
  }
};

export default productMakersReducer;
