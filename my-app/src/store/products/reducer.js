import PRODUCTS from "./data";

const INITIAL_STATE = {
  products: PRODUCTS,
};

const productReducer = (state = INITIAL_STATE) => {
  switch (true) {
    default:
      return state;
  }
};

export default productReducer;
